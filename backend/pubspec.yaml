name: backend
description: Lead Management Platform API (Dart Frog)
version: 1.0.0+1
publish_to: none

environment:
  sdk: ^3.11.0

dependencies:
  bcrypt: ^1.1.3
  dart_frog: ^1.1.0
  dart_jsonwebtoken: ^2.14.0
  dotenv: ^4.2.0
  postgres: ^3.5.0

dev_dependencies:
  dart_frog_lint: ^0.1.0
  http: ^1.2.0
  mocktail: ^1.0.0
  test: ^1.0.0
